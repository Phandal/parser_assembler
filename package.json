{
  "name": "360",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc",
    "test": "tsx --test-reporter spec --experimental-test-module-mocks --test tests/**/*.ts",
    "test:only": "tsx --test-reporter spec --experimental-test-module-mocks --test-only --test tests/**/*.ts",
    "test:coverage": "tsx --test-reporter spec --experimental-test-module-mocks --experimental-test-coverage --test tests/**/*.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/node": "^24.7.1"
  },
  "dependencies": {
    "@types/papaparse": "^5.3.16",
    "papaparse": "^5.5.3",
    "tsx": "^4.20.6"
  }
}
